argtable3/test_apps:
  enable:
    - if: IDF_TARGET in ["esp32", "esp32c3"]
      reason: "Sufficient to test on one Xtensa and one RISC-V target"
    - if: IDF_VERSION_MAJOR >= 6
      reason: "Avoid possible conflicts with esp-idf version of argtable3 removed in 6.0"