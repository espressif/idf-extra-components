esp_linenoise/test_apps:
  disable:
    - if: IDF_TARGET != "linux"
      reason: "Sufficient to test on Linux target"
    - if: IDF_VERSION_MAJOR < 6
      reason: "linenoise is a built-in component in esp-idf version < 6.0"
