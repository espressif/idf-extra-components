# RX8130CE get-started example

This example prints the default policy without touching hardware. The default
keeps backup charging disabled and low-voltage detection enabled. Use the
component with an existing ESP-IDF `i2c_master` bus and set the calendar before
using timestamps.
