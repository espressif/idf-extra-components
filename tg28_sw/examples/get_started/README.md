# TG28_SW get-started example

This example prints the default I2C configuration without touching hardware.
The component owns only its I2C device handle; a board or application must
own the bus, rail sequencing, VBUS policy, and battery-specific model data.
