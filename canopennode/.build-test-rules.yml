canopennode/examples/heartbeat:
  disable:
    - if: SOC_TWAI_SUPPORTED != 1
      reason: CANopenNode requires TWAI driver
    - if: (IDF_VERSION_MAJOR < 5) or (IDF_VERSION_MAJOR == 5 and IDF_VERSION_MINOR < 5)
      reason: IDF version below v5.5.4 is not supported
