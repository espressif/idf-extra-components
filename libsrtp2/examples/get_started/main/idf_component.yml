## Build-check app for libsrtp2.
## During CI / local build, override_path points at the parent repo so we
## consume the in-tree component instead of the registry version.
dependencies:
  vikramdattu/libsrtp2:
    version: "*"
    override_path: ../../..
