[sections:esp_commands]
entries:
    .esp_commands

[scheme:esp_commands_default]
entries:
    esp_commands -> flash_rodata

[mapping:esp_commands]
archive: *
entries:
    * (esp_commands_default);
    esp_commands -> flash_rodata KEEP() SORT(name) SURROUND(esp_commands)
