cbor/host_test:
  enable:
    - if: IDF_TARGET == "linux"
  disable:
    - if: IDF_VERSION_MAJOR <= 5 and IDF_VERSION_MINOR <= 4

cbor/examples/cbor:
  enable:
    - if: IDF_TARGET in ["esp32s3", "esp32c3"]
      reason: "Sufficient to test on representative Xtensa and RISC-V targets"
